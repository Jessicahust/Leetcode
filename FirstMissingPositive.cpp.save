#include<iostream>
using namespace std;

class Solution {
public:
    int firstMissingPositive(int A[], int n) {
        if(n==0)
            return 1;
        int i=1;
        while(i<n)
        {
            if(A[i]>0&&A[i]!=i&&A[A[i]]!=A[i])
                swap(A[i],A[A[i]]);
            else
                i++;
        }
        for(i=1;i<n;i++)
            if(A)
    }
};
