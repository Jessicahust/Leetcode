#include<iostream>
using namespace std;

class Solution {
public:
    int maxSubArray(int A[], int n) {
        int maxSum=0;
        int sum=0;
        if(n==0)
            return 0;
        int i;
        for(i=0;i<n;i++)
        {
            sum+=A[i];
            if(maxSu)
        }
    }
};
