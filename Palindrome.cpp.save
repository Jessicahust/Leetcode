#include<iostream>
#include<cctype>
#include<string>
using namespace std;

class Solution {
public:
    bool isPalindrome(string s) {
        int i=0;
        int j=s.length()-1;
        while(i<=j)
        {
            if(isalnum(s[i])&&isalnum(s[j]))
            {
                if(isalpha(s[i])&&isalpha(s[j]))
                {
                    char c1=tolower(s[i]);
                    char c2=tolower(s[j]);
                    
                }
                if
            }
            else if(isalnum(s[i]))
            {
                j--;
            }
            else if(isalnum(s[j]))
            {
                i++;
            }
            else
            {
                i++;
                j--;
            }
        }
        if(i>j)
            return true;
        else
            return false;
    }
};
