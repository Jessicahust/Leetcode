#include<iostream>
#include<vector>
#include<set>
using namespace std;

class Solution
{
public:
    vector<int> searchRange(int A[], int n, int target)
    {
        if(n==0)
            return vector<int>{-1,-1};
        if(A[0]==A[n-1])
            return vector<int> {0,n-1};
        int mid;
        int left=0;
        int right=n-1;
        while(left<=right)
        {
            mid=(left+right)/2;
            if(A[mid]<=target)
            {
                left=m+1;
            }
            else
                right=m-1;
        }
        
    }
};
